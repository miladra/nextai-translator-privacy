// Telemetry and analytics have been removed to protect user privacy.
// This app intentionally sends data only to configured AI provider endpoints (OpenAI, Azure, Cohere, Claude, etc.).
// No error tracking, session replay, or analytics are collected or sent to third parties.

export async function setupAnalysis() {
    // No-op: telemetry disabled
}

export async function doSetupAnalysis() {
    // No-op: telemetry disabled
}
